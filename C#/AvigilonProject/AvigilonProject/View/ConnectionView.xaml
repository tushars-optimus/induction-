<Window x:Class="AvigilonProject.Model.ConnectionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Local="clr-namespace:AvigilonProject"
        Title="ConnectionView" Height="500" Width="500" WindowStartupLocation="CenterScreen" >
    <Window.Resources>
        <Local:ViewModelLocator x:Key="Locator" />
    </Window.Resources>
    <Grid DataContext="{Binding AvigilonIpAddViewModel,Source={StaticResource Locator }}">
        <GroupBox Focusable="True"  Padding="2" Header="Avigilon" Grid.Row="0" Margin="20,0,0,0" Name="avigilon" >
            <StackPanel>
                <Label Content="IP"/>
                <TextBox Text="{Binding IPs.IP,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"></TextBox>
                <Button Command="{Binding Saves}" Content="Add" Width="60" Margin="25" ></Button>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
